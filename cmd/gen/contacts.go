// Code generated by internal/gen from the HostTracker OpenAPI document. DO NOT EDIT.
//
// Regenerate with ./scripts/regen.sh.

package gen

import "github.com/HostTracker/cli/internal/htcli"

func init() {
	htcli.RegisterGroup(htcli.Group{
		Name:  "contacts",
		Tag:   "Contacts",
		Short: "Notification contacts, contact groups and confirmations",
	})

	htcli.Register(htcli.Operation{
		Group:       "contacts",
		Name:        "bulk-delete",
		ID:          "bulkDeleteContact",
		Method:      "POST",
		Path:        "/contact/bulk-delete",
		Summary:     "Delete every contact a filter selects, as an asynchronous job.",
		Description: "Resolves the filter to a concrete set of contacts, answers with a job id and the number of contacts accepted, and deletes them one at a time - each in its own transaction, each with its own receipt of what was cascaded away. The set is fixed at submission: a contact that starts matching the filter afterwards is not touched. Check what a filter selects first with bulk-delete-validate; this call then acts on it, with no token to echo back. A filter that narrows by nothing is refused rather than taken to mean the whole account, and a selection larger than one operation carries is refused rather than trimmed. An Idempotency-Key is mandatory: this call answers 202 and then works asynchronously, so a retry after a timeout would otherwise start a second job over the same targets. The 202 carries a Retry-After header saying how long to wait before the first poll, sized on how much work was accepted.",
		Params: []htcli.Param{
			{Name: "Idempotency-Key", Flag: "idempotency-key", In: "header", Type: "string", Required: true, Help: "Required. A client-chosen key, unique per logical request."},
		},
		Body:         true,
		BodyRequired: true,
	})

	htcli.Register(htcli.Operation{
		Group:        "contacts",
		Name:         "bulk-delete-validate",
		ID:           "bulkDeleteValidateContact",
		Method:       "POST",
		Path:         "/contact/bulk-delete-validate",
		Summary:      "Check which contacts a delete filter selects, without deleting anything.",
		Description:  "Resolves a filter and answers how many contacts it selects together with a sample of them, identified by name and address. Nothing is written. It is the verification step of a bulk delete: run it, show a human what is about to go, then send the same filter to the delete itself, which acts on it directly. The answer is a snapshot - contacts created or removed between the two calls change what the delete resolves, which is why the delete reports its own count as well.",
		Body:         true,
		BodyRequired: true,
	})

	htcli.Register(htcli.Operation{
		Group:       "contacts",
		Name:        "bulk-validate",
		ID:          "bulkValidateContact",
		Method:      "POST",
		Path:        "/contact/bulk-validate",
		Summary:     "Check what a contact batch would do, without writing anything.",
		Description: "Takes the same body as the bulk write and answers a verdict per item instead of applying it: whether the item is valid, the errors it would be refused with, and whether the account's package has room for it - it fits, it would be created disabled, or it would fail. Nothing is written and no Idempotency-Key is needed. Use it to pre-flight an import before spending a key on it, since the write applies items one at a time and can partially succeed.",
		Body:        true,
	})

	htcli.Register(htcli.Operation{
		Group:       "contacts",
		Name:        "bulk-write",
		ID:          "bulkWriteContact",
		Method:      "POST",
		Path:        "/contact/bulk",
		Summary:     "Create, update and delete many contacts as one asynchronous job.",
		Description: "Submits a batch of contact creations, updates and deletions as a single job and answers immediately with a job id to poll for per-item results. Items are applied one at a time, each in its own transaction, so a batch can partially succeed and the job reports exactly which items landed. Check a batch in advance with bulk-validate. A create the account's package has no room for is created paused rather than refused, unless onOverlimit says otherwise. An Idempotency-Key is mandatory, because a retry would duplicate rows and re-send paid confirmation messages. The 202 carries a Retry-After header saying how long to wait before the first poll, sized on how much work was accepted.",
		Params: []htcli.Param{
			{Name: "Idempotency-Key", Flag: "idempotency-key", In: "header", Type: "string", Required: true, Help: "Required. A client-chosen key, unique per logical request."},
		},
		Body: true,
	})

	htcli.Register(htcli.Operation{
		Group:       "contacts",
		Name:        "create",
		ID:          "createContact",
		Method:      "POST",
		Path:        "/contact",
		Summary:     "Create a contact, or bind to the matching one that already exists.",
		Description: "Creates a delivery contact and, for a channel that needs confirming, issues and sends its confirmation code in the same call. When a contact with the same type, address, gateway and alert delay already exists, the request binds to it instead of creating a duplicate - and the status says which happened, 201 for a new row and 200 for a bind. Alert and report subscriptions can be wired in the same request, riding this same contact:write.",
		Params: []htcli.Param{
			{Name: "Idempotency-Key", Flag: "idempotency-key", In: "header", Type: "string", Help: "Required for some requests, optional for the rest - see this operation's description for the condition."},
		},
		Body:         true,
		BodyRequired: true,
	})

	htcli.Register(htcli.Operation{
		Group:       "contacts",
		Name:        "create-group",
		ID:          "createContactGroup",
		Method:      "POST",
		Path:        "/contact/group",
		Summary:     "Create a contact group.",
		Description: "Creates a named preset of contacts and their event selections. The name is unique per account (case-insensitively); at least one contact with at least one event is required; every contact must be the account's own. Events span both subscription kinds in one vocabulary - the three alert types and the five report frequencies.",
		Params: []htcli.Param{
			{Name: "Idempotency-Key", Flag: "idempotency-key", In: "header", Type: "string", Help: "Optional. A client-chosen key, unique per logical request."},
		},
		Body:         true,
		BodyRequired: true,
	})

	htcli.Register(htcli.Operation{
		Group:       "contacts",
		Name:        "delete",
		ID:          "deleteContact",
		Method:      "DELETE",
		Path:        "/contact/{id}",
		Summary:     "Delete a contact and receive a receipt of what was removed.",
		Description: "Deletes a contact along with every alert and report subscription pointing at it, and answers with a receipt counting what was cascaded away. Use it to retire a delivery channel; the counts exist nowhere else once the delete has run, which is why the response carries a body rather than nothing.",
		Args: []htcli.Arg{
			{Name: "id", Help: "the contact id"},
		},
		Params: []htcli.Param{
			{Name: "Idempotency-Key", Flag: "idempotency-key", In: "header", Type: "string", Help: "Optional. A client-chosen key, unique per logical request."},
		},
	})

	htcli.Register(htcli.Operation{
		Group:       "contacts",
		Name:        "delete-group",
		ID:          "deleteContactGroup",
		Method:      "DELETE",
		Path:        "/contact/group/{id}",
		Summary:     "Delete a contact group.",
		Description: "Removes the group and its membership rows. Deleting a group never touches any live subscription - membership was a preset, not a subscription. 404 for a group that does not exist or belongs to another account, so a 200 always means it existed and is gone. The receipt names the group and counts the contacts its preset listed; the contacts themselves are untouched.",
		Args: []htcli.Arg{
			{Name: "id", Help: "the group id"},
		},
		Params: []htcli.Param{
			{Name: "Idempotency-Key", Flag: "idempotency-key", In: "header", Type: "string", Help: "Optional. A client-chosen key, unique per logical request."},
		},
	})

	htcli.Register(htcli.Operation{
		Group:       "contacts",
		Name:        "get",
		ID:          "getContact",
		Method:      "GET",
		Path:        "/contact/{id}",
		Summary:     "Retrieve one contact by id.",
		Description: "Returns one contact in exactly the row shape the list endpoint uses, including its delivery address, confirmation state, alert delay and - for a channel that is charged per message - the price of one send. Use it when the id is already known rather than filtering the list down to it. A contact that does not exist and one belonging to another account answer the same not-found.",
		Args: []htcli.Arg{
			{Name: "id", Help: "the contact id"},
		},
		Params: []htcli.Param{
			{Name: "expand", Flag: "expand", In: "query", Type: "array", Items: "string", Enum: []string{"subscription", "template", "group", "summary", "count"}, Help: "Comma-separated names of the extra blocks to embed."},
			{Name: "fields", Flag: "fields", In: "query", Type: "array", Items: "string", Enum: []string{"id", "type", "name", "address", "confirmed", "overlimited", "alertDelay", "sendCost", "gateway", "language", "groupedAlerts", "billingNotifications", "sendNews", "mimeType", "httpHeaders", "templates", "activePeriod", "botId", "created", "updated", "subscription", "groups"}, Help: "Which top-level members to keep on each row - fields=id,name."},
		},
	})

	htcli.Register(htcli.Operation{
		Group:       "contacts",
		Name:        "get-group",
		ID:          "getContactGroup",
		Method:      "GET",
		Path:        "/contact/group/{id}",
		Summary:     "Read one contact group.",
		Description: "Returns a single contact group with its full membership snapshot, in exactly the list's row shape. A group that does not exist and one that belongs to another account answer the same 404.",
		Args: []htcli.Arg{
			{Name: "id", Help: "the group id"},
		},
		Params: []htcli.Param{
			{Name: "fields", Flag: "fields", In: "query", Type: "array", Items: "string", Enum: []string{"id", "name", "created", "items"}, Help: "Which top-level members to keep on each row - fields=id,name."},
		},
	})

	htcli.Register(htcli.Operation{
		Group:       "contacts",
		Name:        "list",
		ID:          "listContact",
		Method:      "GET",
		Path:        "/contact",
		Summary:     "List the account's contacts, filtered and cursor-paginated.",
		Description: "Returns a page of contacts - the addresses alerts and reports are delivered to - narrowed by id, type, confirmation state or a free-text search over name and address. Use expand to embed each contact's subscriptions, its message template or the groups it belongs to, and updatedSince for an incremental poll. Fetch one contact by id when only one is wanted. Order it with sort=created|name|address, optionally suffixed :asc or :desc (sort=name:desc); without a suffix created reads newest-first and the text columns A to Z. There is no separate order parameter.",
		Params: []htcli.Param{
			{Name: "confirmed", Flag: "confirmed", In: "query", Type: "boolean", Help: "Restrict to confirmed, or to unconfirmed."},
			{Name: "cursor", Flag: "cursor", In: "query", Type: "string", Help: "Opaque cursor from a previous response's nextCursor."},
			{Name: "expand", Flag: "expand", In: "query", Type: "array", Items: "string", Enum: []string{"subscription", "template", "group", "summary", "count"}, Help: "Comma-separated names of the extra blocks to embed."},
			{Name: "fields", Flag: "fields", In: "query", Type: "array", Items: "string", Enum: []string{"id", "type", "name", "address", "confirmed", "overlimited", "alertDelay", "sendCost", "gateway", "language", "groupedAlerts", "billingNotifications", "sendNews", "mimeType", "httpHeaders", "templates", "activePeriod", "botId", "created", "updated", "subscription", "groups"}, Help: "Which top-level members to keep on each row - fields=id,name."},
			{Name: "id", Flag: "id", In: "query", Type: "array", Items: "string", Explode: true, Help: "Explicit ids. A PRESENT-but-empty value selects NOTHING; an absent parameter is no filter."},
			{Name: "limit", Flag: "limit", In: "query", Type: "integer", Help: "Rows to return."},
			{Name: "q", Flag: "q", In: "query", Type: "string", Help: "Case-insensitive substring over name AND address."},
			{Name: "sort", Flag: "sort", In: "query", Type: "string", Enum: []string{"created", "name", "address", "created:asc", "created:desc", "name:asc", "name:desc", "address:asc", "address:desc"}, Help: "created | name | address, optionally suffixed with :asc or :desc (sort=name:desc)."},
			{Name: "type", Flag: "type", In: "query", Type: "array", Items: "string", Enum: []string{"email", "sms", "voiceCall", "http", "telegram", "viber", "facebook", "googleChat", "discord", "webPush", "skype"}, Explode: true, Help: "One or more contact-type tokens; a row matching any of them is returned."},
			{Name: "updatedSince", Flag: "updated-since", In: "query", Type: "string", Help: "Return only what changed since this point - either Unix seconds, or the syncCursor a previous response returned."},
		},
		QueryPath: "/contact/q",
		Paged:     true,
	})

	htcli.Register(htcli.Operation{
		Group:       "contacts",
		Name:        "list-group",
		ID:          "listContactGroup",
		Method:      "GET",
		Path:        "/contact/group",
		Summary:     "List the account's contact groups.",
		Description: "Returns every contact group with its full membership snapshot - each member as the contact's identifying projection plus its event selection. A group is a PASSIVE preset: applying one means reading it and writing the subscriptions you want through the subscription endpoints; membership by itself subscribes nothing. Sortable: sort=name|created, optionally suffixed :asc or :desc (default name ascending, the list's shipped order).",
		Params: []htcli.Param{
			{Name: "cursor", Flag: "cursor", In: "query", Type: "string", Help: "Opaque cursor from a previous response's nextCursor."},
			{Name: "fields", Flag: "fields", In: "query", Type: "array", Items: "string", Enum: []string{"id", "name", "created", "items"}, Help: "Which top-level members to keep on each row - fields=id,name."},
			{Name: "limit", Flag: "limit", In: "query", Type: "integer", Help: "Rows to return."},
			{Name: "sort", Flag: "sort", In: "query", Type: "string", Enum: []string{"name", "created", "name:asc", "name:desc", "created:asc", "created:desc"}, Help: "name | created, optionally suffixed :asc/:desc."},
		},
		QueryPath: "/contact/group/q",
		Paged:     true,
	})

	htcli.Register(htcli.Operation{
		Group:       "contacts",
		Name:        "list-type",
		ID:          "listContactType",
		Method:      "GET",
		Path:        "/contact/type",
		Summary:     "List the contact types, their gateways and their capabilities.",
		Description: "Returns the catalogue of contact types: which can be created directly, which need confirmation, which can receive scheduled reports, the gateways each offers, and the alert delays the account may choose from. Use it to build a contact form or to validate a type before a create - the creatable set is not fixed forever, so reading it beats hard-coding it.",
		Params: []htcli.Param{
			{Name: "cursor", Flag: "cursor", In: "query", Type: "string", Help: "Opaque cursor from a previous response's nextCursor."},
			{Name: "limit", Flag: "limit", In: "query", Type: "integer", Help: "Rows to return."},
		},
		QueryPath: "/contact/type/q",
		Paged:     true,
	})

	htcli.Register(htcli.Operation{
		Group:       "contacts",
		Name:        "resend-confirmation",
		ID:          "resendContactConfirmation",
		Method:      "POST",
		Path:        "/contact/{id}/confirmation",
		Summary:     "Resend the confirmation code for an unconfirmed contact.",
		Description: "Reissues the confirmation code for a contact, for when the code sent at creation never arrived or its validity window has passed. A code that is still valid is sent again unchanged rather than replaced, so a resend can never invalidate a code the recipient is already reading. Resends are rate-limited per contact and a refusal names when to try again.",
		Args: []htcli.Arg{
			{Name: "id", Help: "the contact id"},
		},
		Params: []htcli.Param{
			{Name: "Idempotency-Key", Flag: "idempotency-key", In: "header", Type: "string", Help: "Optional. A client-chosen key, unique per logical request."},
		},
	})

	htcli.Register(htcli.Operation{
		Group:       "contacts",
		Name:        "set-groups",
		ID:          "setContactGroups",
		Method:      "PUT",
		Path:        "/contact/{id}/group",
		Summary:     "Set the contact groups one contact belongs to.",
		Description: "Replaces a contact's whole group membership in one call: the body names every group the contact should be in, so a group it omits is left and an empty list removes the contact from all of them. Reach for it instead of patching each group in turn - that costs one write per group and rewrites memberships of contacts the caller was not changing. Each entry may name the events to join with; omit them to join with the events the group already carries. A group is a passive preset either way, so joining one subscribes the contact to nothing by itself.",
		Args: []htcli.Arg{
			{Name: "id", Help: "the contact id"},
		},
		Params: []htcli.Param{
			{Name: "Idempotency-Key", Flag: "idempotency-key", In: "header", Type: "string", Help: "Optional. A client-chosen key, unique per logical request."},
		},
		Body:         true,
		BodyRequired: true,
	})

	htcli.Register(htcli.Operation{
		Group:       "contacts",
		Name:        "test",
		ID:          "testContact",
		Method:      "POST",
		Path:        "/contact/{id}/test",
		Summary:     "Send a real test alert to a confirmed contact.",
		Description: "Sends a test notification through the same delivery pipeline production alerts use, so it proves actual delivery rather than only formatting, and reports the outcome synchronously rather than handing back a job. Use it while setting an integration up. Test sends are rate-limited, and a contact that is not yet confirmed or is suppressed by the account's plan is refused.",
		Args: []htcli.Arg{
			{Name: "id", Help: "the contact id"},
		},
		Params: []htcli.Param{
			{Name: "Idempotency-Key", Flag: "idempotency-key", In: "header", Type: "string", Help: "Optional. A client-chosen key, unique per logical request."},
		},
		Body: true,
	})

	htcli.Register(htcli.Operation{
		Group:       "contacts",
		Name:        "update",
		ID:          "updateContact",
		Method:      "PATCH",
		Path:        "/contact/{id}",
		Summary:     "Partially update a contact and get the updated contact back.",
		Description: "Updates one or more members of a contact; a member the body omits stays as it was, and an explicit null clears an optional one. Changing the delivery address resets confirmation and sends a fresh code, because the new address has never proved it belongs to you. A NEW email address is also checked for deliverability, exactly as on create: a domain with no mail host is refused as `validation_failed` with reason `undeliverable_domain` (fail-open on DNS trouble; a body that does not touch `address` is never checked). A contact's type cannot change after creation - create another contact instead.",
		Args: []htcli.Arg{
			{Name: "id", Help: "the contact id"},
		},
		Params: []htcli.Param{
			{Name: "Idempotency-Key", Flag: "idempotency-key", In: "header", Type: "string", Help: "Optional. A client-chosen key, unique per logical request."},
		},
		Body: true,
	})

	htcli.Register(htcli.Operation{
		Group:       "contacts",
		Name:        "update-group",
		ID:          "updateContactGroup",
		Method:      "PATCH",
		Path:        "/contact/group/{id}",
		Summary:     "Change a contact group's name and/or replace its membership.",
		Description: "Patches a group: send `name` to rename, `items` to REPLACE the whole membership snapshot, or both. A group is a snapshot, so membership has no per-row diff - a present items member is the exact desired set. Answers with the group as it now stands.",
		Args: []htcli.Arg{
			{Name: "id", Help: "the group id"},
		},
		Params: []htcli.Param{
			{Name: "Idempotency-Key", Flag: "idempotency-key", In: "header", Type: "string", Help: "Optional. A client-chosen key, unique per logical request."},
		},
		Body: true,
	})

	htcli.Register(htcli.Operation{
		Group:       "contacts",
		Name:        "verify",
		ID:          "verifyContact",
		Method:      "POST",
		Path:        "/contact/{id}/confirmation/verify",
		Summary:     "Confirm a contact with the code it received.",
		Description: "Submits the code delivered to a contact and, when it is correct and still valid, marks the contact confirmed and answers with the contact as it now stands. A wrong or expired code is refused with how many attempts remain and when the code expires, so a client can tell 'try again' from 'ask for a new one'. Confirming an already-confirmed contact is reported distinctly from a fresh success.",
		Args: []htcli.Arg{
			{Name: "id", Help: "the contact id"},
		},
		Params: []htcli.Param{
			{Name: "Idempotency-Key", Flag: "idempotency-key", In: "header", Type: "string", Help: "Optional. A client-chosen key, unique per logical request."},
		},
		Body:         true,
		BodyRequired: true,
	})
}
