// Code generated by internal/gen from the HostTracker OpenAPI document. DO NOT EDIT.
//
// Regenerate with ./scripts/regen.sh.

package gen

import "github.com/HostTracker/cli/internal/htcli"

func init() {
	htcli.RegisterGroup(htcli.Group{
		Name:  "incidents",
		Tag:   "Incidents",
		Short: "Downtime incidents, their checks and their comments",
	})

	htcli.Register(htcli.Operation{
		Group:       "incidents",
		Name:        "comment",
		ID:          "commentIncident",
		Method:      "POST",
		Path:        "/monitor/incident/{id}/comment",
		Summary:     "Annotate an incident and get the incident back.",
		Description: "Attaches a short comment to an incident and answers with the incident as it now stands, so no follow-up read is needed. Use it to record a cause or a resolution alongside the outage itself. Only the monitor's owner may comment - read access through a publicly shared monitor is not enough - and the write is convergent, so a retried submission changes nothing twice.",
		Args: []htcli.Arg{
			{Name: "id", Help: "the incident id"},
		},
		Params: []htcli.Param{
			{Name: "Idempotency-Key", Flag: "idempotency-key", In: "header", Type: "string", Help: "Optional. A client-chosen key, unique per logical request."},
		},
		Body:         true,
		BodyRequired: true,
	})

	htcli.Register(htcli.Operation{
		Group:       "incidents",
		Name:        "get",
		ID:          "getIncident",
		Method:      "GET",
		Path:        "/monitor/incident/{id}",
		Summary:     "Get one incident, with the transitions that opened and closed it.",
		Description: "Returns a single incident including the transition that opened it, the transition that closed it if it is resolved, and the per-location verdicts recorded at each. Use it when an incident id is already in hand and the whole timeline is wanted; the list endpoint returns the same rows but is the wrong tool for one. The response carries the underlying check ids, so a result can be read directly from here. Each timeline entry also carries the assertion rules that failed and the policy codes that were violated, when the check recorded any. Nothing is embedded by default: ask for expand=monitor for the monitor's identifying projection, and expand=monitor.settings / monitor.subscription / monitor.lastIncident / monitor.maintenance to embed the monitor's own blocks inside it. expand=recheck adds the episode-level constellation beside the timeline.",
		Args: []htcli.Arg{
			{Name: "id", Help: "the incident id"},
		},
		Params: []htcli.Param{
			{Name: "expand", Flag: "expand", In: "query", Type: "array", Items: "string", Enum: []string{"monitor", "monitor.settings", "monitor.subscription", "monitor.lastIncident", "monitor.maintenance", "recheck", "count"}, Help: "Comma-separated names of the extra blocks to embed."},
			{Name: "fields", Flag: "fields", In: "query", Type: "array", Items: "string", Enum: []string{"id", "monitorId", "monitor", "start", "end", "durationSec", "state", "severity", "cause", "underMaintenance", "comment", "checkCount", "timeline", "recheck"}, Help: "Which top-level members to keep on each row - fields=id,name."},
		},
	})

	htcli.Register(htcli.Operation{
		Group:       "incidents",
		Name:        "list",
		ID:          "listIncident",
		Method:      "GET",
		Path:        "/monitor/incident",
		Summary:     "List down-episodes across the account or a monitor selection.",
		Description: "Returns a page of incidents - the episodes between a monitor going down and coming back - narrowed by monitor id (monitor=), address (url=, with like=true for substring match) or free text (q=), time window, severity or whether they are still open. The three monitor filters combine rather than adding to each other, exactly as they do on the monitor list, and with none of them this is the account's own incident feed over any window - an episode row exists per state change, so it stays small. Order it with sort=monitor to group the page by monitor (name A to Z, newest-first inside each) instead of the default sort=time. Each row carries its cause, duration, severity band and whether it fell inside a maintenance window. Use it instead of scanning raw results when the question is about outages rather than checks. Nothing is embedded by default: ask for expand=monitor for the monitor's identifying projection, and expand=monitor.settings / monitor.subscription / monitor.lastIncident / monitor.maintenance to embed the monitor's own blocks inside it. expand=recheck adds the constellation that opened each episode: the location that detected it, the locations that confirmed it grouped by the error each saw, and the locations that still saw the target up.",
		Params: []htcli.Param{
			{Name: "cursor", Flag: "cursor", In: "query", Type: "string", Help: "Opaque cursor from a previous response's nextCursor."},
			{Name: "expand", Flag: "expand", In: "query", Type: "array", Items: "string", Enum: []string{"monitor", "monitor.settings", "monitor.subscription", "monitor.lastIncident", "monitor.maintenance", "recheck", "count"}, Help: "Comma-separated names of the extra blocks to embed."},
			{Name: "fields", Flag: "fields", In: "query", Type: "array", Items: "string", Enum: []string{"id", "monitorId", "monitor", "start", "end", "durationSec", "state", "severity", "cause", "underMaintenance", "comment", "checkCount", "timeline", "recheck"}, Help: "Which top-level members to keep on each row - fields=id,name."},
			{Name: "from", Flag: "from", In: "query", Type: "integer", Help: "The start of the time window, in Unix seconds."},
			{Name: "like", Flag: "like", In: "query", Type: "boolean", Help: "true switches url= to substring match."},
			{Name: "limit", Flag: "limit", In: "query", Type: "integer", Help: "Rows to return."},
			{Name: "monitor", Flag: "monitor", In: "query", Type: "array", Items: "string", Explode: true, Help: "Monitor ids, ANY-OF - a NARROWING filter ANDed with url=/q=, and optional: omitting every monitor filter reads the whole account's incident feed..."},
			{Name: "q", Flag: "q", In: "query", Type: "string", Help: "Free-text monitor filter: substring over name + address, the same matching GET /monitor's q= performs, ANDed with the other monitor filters."},
			{Name: "severity", Flag: "severity", In: "query", Type: "array", Items: "string", Enum: []string{"minor", "major", "critical"}, Explode: true, Help: "minor | major | critical, ANY-OF."},
			{Name: "sort", Flag: "sort", In: "query", Type: "string", Enum: []string{"time", "time:desc", "monitor", "monitor:asc", "monitor:desc"}, Help: "Which column to order by, optionally with a direction: sort=name or sort=name:desc."},
			{Name: "state", Flag: "state", In: "query", Type: "array", Items: "string", Enum: []string{"open", "resolved"}, Explode: true, Help: "open | resolved - ANY-OF."},
			{Name: "to", Flag: "to", In: "query", Type: "integer", Help: "The end of the time window, in Unix seconds."},
			{Name: "url", Flag: "url", In: "query", Type: "array", Items: "string", Explode: true, Help: "Narrow by the monitor's ADDRESS instead of by GUID - exact (case-insensitive) unless like=true."},
		},
		QueryPath: "/monitor/incident/q",
		Paged:     true,
	})

	htcli.Register(htcli.Operation{
		Group:       "incidents",
		Name:        "list-check",
		ID:          "listIncidentCheck",
		Method:      "GET",
		Path:        "/monitor/incident/{id}/check",
		Summary:     "List the failing checks recorded inside one incident.",
		Description: "Returns a page of the failed checks that occurred during one incident, each linking to its full result and page snapshot. Use it to see exactly what happened check by check during an outage, rather than re-deriving the episode from the raw result feed. Availability depends on the account's plan, and a plan refusal names the feature it needs. Each row carries the monitor's identifying projection and the recheck constellation behind the failure. Ask for expand=metrics to decode each check's stored measurements and, from the same document, the assertion rules it failed and the policy codes it violated - the per-check counterpart of the detail the incident's own timeline carries for the transitions that opened and closed the episode.",
		Args: []htcli.Arg{
			{Name: "id", Help: "the incident id"},
		},
		Params: []htcli.Param{
			{Name: "cursor", Flag: "cursor", In: "query", Type: "string", Help: "Opaque cursor from a previous response's nextCursor."},
			{Name: "expand", Flag: "expand", In: "query", Type: "array", Items: "string", Enum: []string{"monitor", "monitor.settings", "monitor.subscription", "monitor.lastIncident", "monitor.maintenance", "metrics", "recheck"}, Help: "Comma-separated names of the extra blocks to embed."},
			{Name: "fields", Flag: "fields", In: "query", Type: "array", Items: "string", Enum: []string{"id", "monitorId", "monitor", "at", "durationSec", "state", "checkNumber", "checkCount", "location", "underMaintenance", "error", "hasSnapshot", "snapshotUrl", "metrics", "recheck", "assertFails", "assertEv", "policyViolations"}, Help: "Which top-level members to keep on each row - fields=id,name."},
			{Name: "limit", Flag: "limit", In: "query", Type: "integer", Help: "Rows to return."},
		},
		QueryPath: "/monitor/incident/{id}/check/q",
		Paged:     true,
	})

	htcli.Register(htcli.Operation{
		Group:       "incidents",
		Name:        "list-monitor",
		ID:          "listMonitorIncident",
		Method:      "GET",
		Path:        "/monitor/{monitorId}/incident",
		Summary:     "List one monitor's down-episodes.",
		Description: "Returns a page of the monitor's incidents, newest first, with the same window, severity, state and expansion options as the collection read, and no sort parameter - a single monitor's page has nothing to group by. Use it when the monitor is already known; the collection read at /monitor/incident serves cross-monitor and account-wide questions. Nothing is embedded by default: ask for expand=monitor for the monitor's identifying projection, and expand=monitor.settings / monitor.subscription / monitor.lastIncident / monitor.maintenance to embed the monitor's own blocks inside it. expand=recheck adds the constellation that opened each episode.",
		Args: []htcli.Arg{
			{Name: "monitorId", Help: "the monitor id"},
		},
		Params: []htcli.Param{
			{Name: "cursor", Flag: "cursor", In: "query", Type: "string", Help: "Opaque cursor from a previous response's nextCursor."},
			{Name: "expand", Flag: "expand", In: "query", Type: "array", Items: "string", Enum: []string{"monitor", "monitor.settings", "monitor.subscription", "monitor.lastIncident", "monitor.maintenance", "recheck", "count"}, Help: "Comma-separated names of the extra blocks to embed."},
			{Name: "fields", Flag: "fields", In: "query", Type: "array", Items: "string", Enum: []string{"id", "monitorId", "monitor", "start", "end", "durationSec", "state", "severity", "cause", "underMaintenance", "comment", "checkCount", "timeline", "recheck"}, Help: "Which top-level members to keep on each row - fields=id,name."},
			{Name: "from", Flag: "from", In: "query", Type: "integer", Help: "The start of the time window, in Unix seconds."},
			{Name: "limit", Flag: "limit", In: "query", Type: "integer", Help: "Rows to return."},
			{Name: "severity", Flag: "severity", In: "query", Type: "array", Items: "string", Enum: []string{"minor", "major", "critical"}, Explode: true, Help: "minor | major | critical, ANY-OF."},
			{Name: "state", Flag: "state", In: "query", Type: "array", Items: "string", Enum: []string{"open", "resolved"}, Explode: true, Help: "open | resolved - ANY-OF."},
			{Name: "to", Flag: "to", In: "query", Type: "integer", Help: "The end of the time window, in Unix seconds."},
		},
		QueryPath: "/monitor/{monitorId}/incident/q",
		Paged:     true,
	})
}
