// Code generated by internal/gen from the HostTracker OpenAPI document. DO NOT EDIT.
//
// Regenerate with ./scripts/regen.sh.

package gen

import "github.com/HostTracker/cli/internal/htcli"

func init() {
	htcli.RegisterGroup(htcli.Group{
		Name:    "instant-checks",
		Tag:     "InstantChecks",
		Short:   "One-off checks run on demand, without a monitor",
		Aliases: []string{"checks"},
	})

	htcli.Register(htcli.Operation{
		Group:       "instant-checks",
		Name:        "create",
		ID:          "createInstantCheck",
		Method:      "POST",
		Path:        "/check",
		Summary:     "Start a one-off check against a url or host.",
		Description: "Starts a check that runs immediately rather than on a schedule and answers with the identifiers to poll for its result, plus how long to wait before the first poll. Use it to test a url, verify a fix or run a diagnostic without creating a permanent monitor. When the checking pipeline is unavailable the request is refused outright - a check that was not registered never happened, and answering as if it had would hand back an id that never resolves.",
		Params: []htcli.Param{
			{Name: "Idempotency-Key", Flag: "idempotency-key", In: "header", Type: "string", Help: "Optional. A client-chosen key, unique per logical request."},
		},
		Body:         true,
		BodyRequired: true,
	})

	htcli.Register(htcli.Operation{
		Group:       "instant-checks",
		Name:        "get",
		ID:          "getInstantCheck",
		Method:      "GET",
		Path:        "/check/{dbId}/{id}",
		Summary:     "Fetch the state and result of an instant check.",
		Description: "Returns an instant check's current state and, once it has finished, its per-location results. While it is still running the response says how long to wait before asking again, both in the body and as a header, so a client never has to guess a polling interval. Use the history endpoint to browse past checks rather than this one.",
		Args: []htcli.Arg{
			{Name: "dbId", Help: "the federation the check was registered in"},
			{Name: "id", Help: "the check id"},
		},
		Params: []htcli.Param{
			{Name: "fields", Flag: "fields", In: "query", Type: "array", Items: "string", Enum: []string{"id", "dbId", "state", "url", "type", "created", "doneAt", "retryAfter", "events"}, Help: "Which top-level members to keep on each row - fields=id,name."},
		},
	})

	htcli.Register(htcli.Operation{
		Group:       "instant-checks",
		Name:        "list",
		ID:          "listInstantCheck",
		Method:      "GET",
		Path:        "/check",
		Summary:     "List the account's past instant checks.",
		Description: "Returns a page of the instant checks this account has run, newest first, optionally narrowed by time window or check type. Use it to find or audit a past ad-hoc check; to read one check's outcome, address it directly by the identifiers the start call returned.",
		Params: []htcli.Param{
			{Name: "cursor", Flag: "cursor", In: "query", Type: "string", Help: "Opaque cursor from a previous response's nextCursor."},
			{Name: "fields", Flag: "fields", In: "query", Type: "array", Items: "string", Enum: []string{"id", "dbId", "type", "url", "state", "up", "created", "doneAt"}, Help: "Which top-level members to keep on each row - fields=id,name."},
			{Name: "from", Flag: "from", In: "query", Type: "integer", Help: "The start of the time window, in Unix seconds."},
			{Name: "limit", Flag: "limit", In: "query", Type: "integer", Help: "Rows to return."},
			{Name: "to", Flag: "to", In: "query", Type: "integer", Help: "The end of the time window, in Unix seconds."},
			{Name: "type", Flag: "type", In: "query", Type: "array", Items: "string", Enum: []string{"crawl", "dns", "dnsbl", "http", "ping", "port", "trace", "waterfall", "webRisk", "whois"}, Explode: true, Help: "v2 type tokens. Repeatable and comma-separable."},
		},
		QueryPath: "/check/q",
		Paged:     true,
	})

	htcli.Register(htcli.Operation{
		Group:       "instant-checks",
		Name:        "list-device",
		ID:          "listInstantCheckDevice",
		Method:      "GET",
		Path:        "/check/device",
		Summary:     "List the device profiles a page-loading check can emulate.",
		Description: "Returns the device profiles a waterfall check can be run as - the browser is emulated as that device, which changes the viewport, the pixel density and the user agent the target sees. A row's device is the value the deviceEmulation setting takes, on an instant check and on a monitor alike. Rows arrive in the order a picker should offer them. The same names ride the waterfall row of the instant-check type catalogue; read this endpoint when the profiles themselves are what you are after.",
		Params: []htcli.Param{
			{Name: "cursor", Flag: "cursor", In: "query", Type: "string", Help: "Opaque cursor from a previous response's nextCursor."},
			{Name: "fields", Flag: "fields", In: "query", Type: "array", Items: "string", Enum: []string{"device", "priority"}, Help: "Which top-level members to keep on each row - fields=id,name."},
			{Name: "limit", Flag: "limit", In: "query", Type: "integer", Help: "Rows to return."},
		},
		QueryPath: "/check/device/q",
		Paged:     true,
	})

	htcli.Register(htcli.Operation{
		Group:       "instant-checks",
		Name:        "list-type",
		ID:          "listInstantCheckType",
		Method:      "GET",
		Path:        "/check/type",
		Summary:     "List the instant-check types and their options.",
		Description: "Returns the catalogue of instant-check types with a description, an example target and any per-type options such as device profiles or record types. Use it to discover what can be checked, and with which options, before starting a check.",
		Params: []htcli.Param{
			{Name: "cursor", Flag: "cursor", In: "query", Type: "string", Help: "Opaque cursor from a previous response's nextCursor."},
			{Name: "fields", Flag: "fields", In: "query", Type: "array", Items: "string", Enum: []string{"type", "label", "description", "example", "experimental", "agentRouted", "retryAfter", "estimatedDurationSec", "options"}, Help: "Which top-level members to keep on each row - fields=id,name."},
			{Name: "limit", Flag: "limit", In: "query", Type: "integer", Help: "Rows to return."},
		},
		QueryPath: "/check/type/q",
		Paged:     true,
	})
}
