// Code generated by internal/gen from the HostTracker OpenAPI document. DO NOT EDIT.
//
// Regenerate with ./scripts/regen.sh.

package gen

import "github.com/HostTracker/cli/internal/htcli"

func init() {
	htcli.RegisterGroup(htcli.Group{
		Name:  "jobs",
		Tag:   "Jobs",
		Short: "Long-running batch jobs started by the bulk operations",
	})

	htcli.Register(htcli.Operation{
		Group:       "jobs",
		Name:        "cancel",
		ID:          "cancelJob",
		Method:      "POST",
		Path:        "/job/{id}/cancel",
		Summary:     "Cancel a queued or running asynchronous operation.",
		Description: "Asks a job that has not finished to stop, and answers with its state and the results it produced before stopping. Work already completed is not undone, and the job itself is not removed - it stays readable for the rest of its retention window. Cancelling a job that is already cancelling is safe and changes nothing; a job that has already reached a final state cannot be cancelled and says so.",
		Args: []htcli.Arg{
			{Name: "id", Help: "the job id"},
		},
		Params: []htcli.Param{
			{Name: "cursor", Flag: "cursor", In: "query", Type: "string", Help: "Opaque cursor from a previous response's nextCursor."},
			{Name: "Idempotency-Key", Flag: "idempotency-key", In: "header", Type: "string", Help: "Optional. A client-chosen key, unique per logical request."},
			{Name: "limit", Flag: "limit", In: "query", Type: "integer", Help: "Rows to return."},
		},
	})

	htcli.Register(htcli.Operation{
		Group:       "jobs",
		Name:        "get",
		ID:          "getJob",
		Method:      "GET",
		Path:        "/job/{id}",
		Summary:     "Poll an asynchronous operation's state and per-item results.",
		Description: "Returns the state, progress and a page of per-item results for any asynchronous operation started anywhere on this API, whatever created it. Poll it until the state is terminal. Large result sets page inside the response, so keep following the cursor until none is returned. The scope required is the one the creating operation used, so a token that could start the job can always read it. While the job is still running the response carries a Retry-After header saying how long to wait before polling again; a finished job carries none.",
		Args: []htcli.Arg{
			{Name: "id", Help: "the job id"},
		},
		Params: []htcli.Param{
			{Name: "cursor", Flag: "cursor", In: "query", Type: "string", Help: "Opaque cursor from a previous response's nextCursor."},
			{Name: "fields", Flag: "fields", In: "query", Type: "array", Items: "string", Enum: []string{"id", "kind", "scope", "state", "progress", "summary", "cancelRequested", "created", "startedAt", "finishedAt", "expiresAt", "error", "resultSummary", "interruptedAt", "resumedCount", "results", "nextCursor", "hasMore"}, Help: "Which top-level members to keep on each row - fields=id,name."},
			{Name: "limit", Flag: "limit", In: "query", Type: "integer", Help: "Rows to return."},
		},
		Paged: true,
	})

	htcli.Register(htcli.Operation{
		Group:       "jobs",
		Name:        "list",
		ID:          "listJob",
		Method:      "GET",
		Path:        "/job",
		Summary:     "List this account's recent asynchronous operations.",
		Description: "Returns a page of the asynchronous operations this account has started, newest first, optionally narrowed by kind or state. Reach for it when a job id was lost between starting the work and polling it, or to show what is currently running. Rows carry each job's state, progress and counts but not its per-item results - follow a row's results url to read those. Jobs are readable for seven days, which is what bounds this list.",
		Params: []htcli.Param{
			{Name: "cursor", Flag: "cursor", In: "query", Type: "string", Help: "Opaque cursor from a previous response's nextCursor."},
			{Name: "fields", Flag: "fields", In: "query", Type: "array", Items: "string", Enum: []string{"id", "kind", "scope", "state", "progress", "summary", "cancelRequested", "created", "startedAt", "finishedAt", "expiresAt", "interruptedAt", "resumedCount", "resultsUrl"}, Help: "Which top-level members to keep on each row - fields=id,name."},
			{Name: "kind", Flag: "kind", In: "query", Type: "array", Items: "string", Explode: true, Help: "Job kinds, e.g. monitor.bulkCreate - ANY-OF."},
			{Name: "limit", Flag: "limit", In: "query", Type: "integer", Help: "Rows to return."},
			{Name: "state", Flag: "state", In: "query", Type: "array", Items: "string", Enum: []string{"queued", "running", "succeeded", "partial", "failed", "cancelled", "interrupted"}, Explode: true, Help: "Which lifecycle states to include."},
		},
		QueryPath: "/job/q",
		Paged:     true,
	})

	htcli.Register(htcli.Operation{
		Group:       "jobs",
		Name:        "resume",
		ID:          "resumeJob",
		Method:      "POST",
		Path:        "/job/{id}/resume",
		Summary:     "Continue an asynchronous operation that was interrupted.",
		Description: "Restarts an operation whose state is interrupted - one that stopped because the server it was running on stopped, not because anything about the work failed. Everything it had already done stands, and the continuation skips those items, so resuming is safe and never repeats work. Only an interrupted job can be resumed: one that is still running needs nothing but another poll, and a few kinds cannot be continued at all because the request they were carrying is not stored - those say so and ask you to submit again.",
		Args: []htcli.Arg{
			{Name: "id", Help: "the job id"},
		},
		Params: []htcli.Param{
			{Name: "cursor", Flag: "cursor", In: "query", Type: "string", Help: "Opaque cursor from a previous response's nextCursor."},
			{Name: "Idempotency-Key", Flag: "idempotency-key", In: "header", Type: "string", Help: "Optional. A client-chosen key, unique per logical request."},
			{Name: "limit", Flag: "limit", In: "query", Type: "integer", Help: "Rows to return."},
		},
	})
}
