// Code generated by internal/gen from the HostTracker OpenAPI document. DO NOT EDIT.
//
// Regenerate with ./scripts/regen.sh.

package gen

import "github.com/HostTracker/cli/internal/htcli"

func init() {
	htcli.RegisterGroup(htcli.Group{
		Name:  "account",
		Tag:   "Account",
		Short: "The account, its quota and its usage",
	})

	htcli.Register(htcli.Operation{
		Group:       "account",
		Name:        "get",
		ID:          "getAccount",
		Method:      "GET",
		Path:        "/account",
		Summary:     "Read the account: identity, package, usage, limits and status.",
		Description: "Returns the caller's account in one document - who it is, which package it holds, what it is using, the bounds every other endpoint enforces, and whether monitoring is currently running. Make it the first call an integration ever makes: the limits block is what lets a client size its later requests instead of discovering the bounds by being refused. Expand quota to fold the quota document in and save a second call.",
		Params: []htcli.Param{
			{Name: "expand", Flag: "expand", In: "query", Type: "array", Items: "string", Enum: []string{"quota"}, Help: "Comma-separated names of the extra blocks to embed."},
			{Name: "fields", Flag: "fields", In: "query", Type: "array", Items: "string", Enum: []string{"id", "login", "timezone", "language", "profile", "defaultAgentPools", "flags", "package", "usage", "overlimits", "limits", "badges", "quota"}, Help: "Which top-level members to keep on each row - fields=id,name."},
		},
	})

	htcli.Register(htcli.Operation{
		Group:       "account",
		Name:        "get-quota",
		ID:          "getAccountQuota",
		Method:      "GET",
		Path:        "/account/quota",
		Summary:     "Read the account's current API quota headroom.",
		Description: "Returns how much of each metered quota pool remains and when each window resets. Call it before starting a large batch to confirm there is room, rather than discovering the ceiling partway through. The same numbers ride every response as headers, so a client that reads those does not need this endpoint at all.",
		Params: []htcli.Param{
			{Name: "fields", Flag: "fields", In: "query", Type: "array", Items: "string", Enum: []string{"limit", "used", "remaining", "resetAt", "pools", "tokenCap", "scopes", "apiEnabled"}, Help: "Which top-level members to keep on each row - fields=id,name."},
		},
	})

	htcli.Register(htcli.Operation{
		Group:       "account",
		Name:        "get-usage",
		ID:          "getAccountUsage",
		Method:      "GET",
		Path:        "/account/usage",
		Summary:     "Read the account's current resource consumption per domain.",
		Description: "Returns how much of each resource the account is using - under the singular keys `monitor`, `contact`, `report` (subscriptions) and `maintenance` (windows) - each as `{used, allowed}`, where `allowed: null` means no limit and `0` means a real cap of none. Call it to check headroom before a bulk create. This is the same `usage` block that rides the full `GET /account` object, exposed on its own so a client need not pull the whole account to poll consumption. Distinct from `quota`, which publishes the caps/entitlements you need BEFORE writing.",
		Params: []htcli.Param{
			{Name: "fields", Flag: "fields", In: "query", Type: "array", Items: "string", Enum: []string{"monitor", "contact", "report", "maintenance"}, Help: "Which top-level members to keep on each row - fields=id,name."},
		},
	})

	htcli.Register(htcli.Operation{
		Group:       "account",
		Name:        "list-member",
		ID:          "listAccountMember",
		Method:      "GET",
		Path:        "/account/member",
		Summary:     "List the contacts holding shared access to the account.",
		Description: "Returns a page of the people granted shared access to the account, with the rights each holds and whether the invitation has been accepted. Use it to audit or display who can act on the account's behalf. Access grants themselves are managed elsewhere; this surface is read-only.",
		Params: []htcli.Param{
			{Name: "cursor", Flag: "cursor", In: "query", Type: "string", Help: "Opaque cursor from a previous response's nextCursor."},
			{Name: "fields", Flag: "fields", In: "query", Type: "array", Items: "string", Enum: []string{"id", "userId", "contact", "rights", "state"}, Help: "Which top-level members to keep on each row - fields=id,name."},
			{Name: "limit", Flag: "limit", In: "query", Type: "integer", Help: "Rows to return."},
		},
		QueryPath: "/account/member/q",
		Paged:     true,
	})

	htcli.Register(htcli.Operation{
		Group:       "account",
		Name:        "update",
		ID:          "updateAccount",
		Method:      "PATCH",
		Path:        "/account",
		Summary:     "Change the account's own details.",
		Description: "Updates the members the body names and leaves the rest alone: the account holder's name, company, phone and country, the time zone every schedule on the account is anchored to, the language notifications are written in, and the monitoring locations a new monitor starts with. Send an empty string to clear any of the four text fields, and defaultAgentPools as null to clear every default location. Answers the whole updated account, so nothing has to be re-read to confirm the change.",
		Params: []htcli.Param{
			{Name: "Idempotency-Key", Flag: "idempotency-key", In: "header", Type: "string", Help: "Optional. A client-chosen key, unique per logical request."},
		},
		Body: true,
	})
}
