// Code generated by internal/gen from the HostTracker OpenAPI document. DO NOT EDIT.
//
// Regenerate with ./scripts/regen.sh.

package gen

import "github.com/HostTracker/cli/internal/htcli"

func init() {
	htcli.RegisterGroup(htcli.Group{
		Name:  "maintenance",
		Tag:   "Maintenance",
		Short: "Planned maintenance windows",
	})

	htcli.Register(htcli.Operation{
		Group:       "maintenance",
		Name:        "create",
		ID:          "createMaintenance",
		Method:      "POST",
		Path:        "/maintenance",
		Summary:     "Schedule a maintenance window over an explicit set of monitors.",
		Description: "Creates a maintenance window and answers with it, including the coverage it resolved to. Use it to suppress alerts, and optionally statistics, for planned downtime - disabling the monitors instead would also erase the difference between planned and unplanned outage in the record. Name the monitors it covers in one of two ways, never both: monitorIds with a single suppress when they are all treated the same, or monitors with a suppression each when they are not. A window with neither is refused - a window covering nothing silences nothing. The window is anchored to the time zone supplied, an IANA id, so a recurring window keeps its local hour across a daylight-saving change.",
		Params: []htcli.Param{
			{Name: "Idempotency-Key", Flag: "idempotency-key", In: "header", Type: "string", Help: "Optional. A client-chosen key, unique per logical request."},
		},
		Body:         true,
		BodyRequired: true,
	})

	htcli.Register(htcli.Operation{
		Group:       "maintenance",
		Name:        "delete",
		ID:          "deleteMaintenance",
		Method:      "DELETE",
		Path:        "/maintenance/{id}",
		Summary:     "Cancel a maintenance window and receive a receipt.",
		Description: "Cancels a maintenance window immediately and answers with a receipt describing what was removed, including whether the window was actively suppressing alerts at the moment it was cancelled - which decides whether alerting resumes right now. The receipt is what confirms the cancellation, so no follow-up read is needed.",
		Args: []htcli.Arg{
			{Name: "id", Help: "the maintenance id"},
		},
		Params: []htcli.Param{
			{Name: "Idempotency-Key", Flag: "idempotency-key", In: "header", Type: "string", Help: "Optional. A client-chosen key, unique per logical request."},
		},
	})

	htcli.Register(htcli.Operation{
		Group:       "maintenance",
		Name:        "get",
		ID:          "getMaintenance",
		Method:      "GET",
		Path:        "/maintenance/{id}",
		Summary:     "Read one maintenance window.",
		Description: "Returns a single maintenance window in exactly the shape the list returns it in - which is what makes the Location header a created window answers with worth following, and what makes the row safe to send straight back to an update. A window that does not exist and one that belongs to another account answer the same 404. Nothing is embedded by default: ask for expand=monitor for the monitor's identifying projection, and expand=monitor.settings / monitor.subscription / monitor.lastIncident / monitor.maintenance to embed the monitor's own blocks inside it.",
		Args: []htcli.Arg{
			{Name: "id", Help: "the maintenance id"},
		},
		Params: []htcli.Param{
			{Name: "expand", Flag: "expand", In: "query", Type: "array", Items: "string", Enum: []string{"monitor", "monitor.settings", "monitor.subscription", "monitor.lastIncident", "monitor.maintenance"}, Help: "Comma-separated names of the extra blocks to embed."},
			{Name: "fields", Flag: "fields", In: "query", Type: "array", Items: "string", Enum: []string{"id", "name", "from", "to", "durationSec", "timezone", "recurrence", "enabled", "state", "overlimited", "suppress", "monitorIds", "monitors", "created", "updated"}, Help: "Which top-level members to keep on each row - fields=id,name."},
		},
	})

	htcli.Register(htcli.Operation{
		Group:       "maintenance",
		Name:        "list",
		ID:          "listMaintenance",
		Method:      "GET",
		Path:        "/maintenance",
		Summary:     "List scheduled, active and finished maintenance windows.",
		Description: "Returns a page of maintenance windows, optionally narrowed by time window, state or the monitors they cover. A recurring window is returned once with its recurrence rule rather than expanded into every future occurrence, so a year of weekly maintenance is one row. The envelope carries a sync cursor, so a poll loop can read only what changed. Order it with sort=from (the default, newest window start first) or sort=created, each with an optional :asc/:desc suffix. Every row carries its coverage per monitor in monitors[]; the window-level suppress is present only when every monitor it covers shares one suppression. Nothing is embedded by default: ask for expand=monitor for the monitor's identifying projection, and expand=monitor.settings / monitor.subscription / monitor.lastIncident / monitor.maintenance to embed the monitor's own blocks inside it.",
		Params: []htcli.Param{
			{Name: "cursor", Flag: "cursor", In: "query", Type: "string", Help: "Opaque cursor from a previous response's nextCursor."},
			{Name: "expand", Flag: "expand", In: "query", Type: "array", Items: "string", Enum: []string{"monitor", "monitor.settings", "monitor.subscription", "monitor.lastIncident", "monitor.maintenance"}, Help: "monitor - the identifying projection of the window's monitor set."},
			{Name: "fields", Flag: "fields", In: "query", Type: "array", Items: "string", Enum: []string{"id", "name", "from", "to", "durationSec", "timezone", "recurrence", "enabled", "state", "overlimited", "suppress", "monitorIds", "monitors", "created", "updated"}, Help: "Which top-level members to keep on each row - fields=id,name."},
			{Name: "from", Flag: "from", In: "query", Type: "integer", Help: "Window START lower bound, Unix seconds."},
			{Name: "limit", Flag: "limit", In: "query", Type: "integer", Help: "Rows to return."},
			{Name: "monitor", Flag: "monitor", In: "query", Type: "array", Items: "string", Explode: true, Help: "Only windows covering these monitors."},
			{Name: "sort", Flag: "sort", In: "query", Type: "string", Enum: []string{"from", "created", "from:asc", "from:desc", "created:asc", "created:desc"}, Help: "from (the default) | created, optionally suffixed with :asc or :desc (sort=created:asc)."},
			{Name: "state", Flag: "state", In: "query", Type: "array", Items: "string", Enum: []string{"scheduled", "active", "finished"}, Explode: true, Help: "scheduled | active | finished, any combination."},
			{Name: "to", Flag: "to", In: "query", Type: "integer", Help: "Window START upper bound, Unix seconds."},
			{Name: "updatedSince", Flag: "updated-since", In: "query", Type: "string", Help: "Return only what changed since this point - either Unix seconds, or the syncCursor a previous response returned."},
		},
		QueryPath: "/maintenance/q",
		Paged:     true,
	})

	htcli.Register(htcli.Operation{
		Group:       "maintenance",
		Name:        "list-monitor",
		ID:          "listMonitorMaintenance",
		Method:      "GET",
		Path:        "/monitor/{monitorId}/maintenance",
		Summary:     "List the maintenance windows covering one monitor.",
		Description: "Returns the maintenance windows that cover the monitor in the path, each as its window DEFINITION in exactly the shape the maintenance list returns - a recurring window appears once, never expanded into occurrences. The monitor-side answer to the monitor-maintenance relationship; the same rows can be inlined on a monitor read with expand=maintenance, and the window-side direction is the maintenance list's expand=monitor. Nothing is embedded by default: ask for expand=monitor for the monitor's identifying projection, and expand=monitor.settings / monitor.subscription / monitor.lastIncident / monitor.maintenance to embed the monitor's own blocks inside it.",
		Args: []htcli.Arg{
			{Name: "monitorId", Help: "the monitor id"},
		},
		Params: []htcli.Param{
			{Name: "cursor", Flag: "cursor", In: "query", Type: "string", Help: "Opaque cursor from a previous response's nextCursor."},
			{Name: "expand", Flag: "expand", In: "query", Type: "array", Items: "string", Enum: []string{"monitor", "monitor.settings", "monitor.subscription", "monitor.lastIncident", "monitor.maintenance"}, Help: "Comma-separated names of the extra blocks to embed."},
			{Name: "fields", Flag: "fields", In: "query", Type: "array", Items: "string", Enum: []string{"id", "name", "from", "to", "durationSec", "timezone", "recurrence", "enabled", "state", "overlimited", "suppress", "monitorIds", "monitors", "created", "updated"}, Help: "Which top-level members to keep on each row - fields=id,name."},
			{Name: "limit", Flag: "limit", In: "query", Type: "integer", Help: "Rows to return."},
		},
		QueryPath: "/monitor/{monitorId}/maintenance/q",
		Paged:     true,
	})

	htcli.Register(htcli.Operation{
		Group:       "maintenance",
		Name:        "update",
		ID:          "updateMaintenance",
		Method:      "PATCH",
		Path:        "/maintenance/{id}",
		Summary:     "Reschedule a maintenance window or change what it covers.",
		Description: "Applies a partial update to a maintenance window - its schedule, time zone, name, what it suppresses, or the monitors it covers - and answers with the window as it now stands. Members the body omits are unchanged. Use it rather than deleting and recreating when only part of the configuration moves. Coverage, when sent, REPLACES the previous coverage rather than adding to it: monitors states the whole set with a suppression each, monitorIds with suppress gives every listed monitor the same one, monitorIds alone keeps each already-covered monitor's own suppression, and suppress alone applies one to every monitor the window already covers. Adding a monitor with monitorIds alone to a window whose monitors do not all share one suppression is refused, because there would be nothing to give the newcomer.",
		Args: []htcli.Arg{
			{Name: "id", Help: "the maintenance id"},
		},
		Params: []htcli.Param{
			{Name: "Idempotency-Key", Flag: "idempotency-key", In: "header", Type: "string", Help: "Optional. A client-chosen key, unique per logical request."},
		},
		Body: true,
	})
}
